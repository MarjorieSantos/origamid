//função para verificar se o valor é truthy

function verificar(idade){
    return !!idade;
}

//função matematica que retorne o perimetro do quadrado (soma dos quatro lados)
    function perimetro(lado){
        return lado * 4
    }

//função que retorne seu nome completo (nome + sobrenome)
    function nomeCompleto(nome, sobrenome){
        return `${nome} ${sobrenome}`; //dá espaço automaticamente com o template string
    }

//verifica se o numero é par
    function isEven(numero){
        var modulo = numero % 2
        if (modulo === 0){
            return turn
        } else{
            return false
        }
    }


//retorne o tipo de dado que é passado nela (typeof)
    function tipoDado(dado){
     return typeof dado;
 }

 //mostrar o nome no console quando a função ocorrer
    addEventListener("scroll"), function(){
     console.log ("Marjorie")
 }