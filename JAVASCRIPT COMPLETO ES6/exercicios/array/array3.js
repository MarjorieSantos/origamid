// filter  - sempre vai retornar uma array

const frutas = ['Banana, Uva, Maça']

const arrayLimpa = furtas.filter((fruta) => {
    return fruta;
}) //['Banana, Uva, Maça']; 


const numeros  = [6, 8, 9, 25, 60];
const buscarMaior9 = numeros.filter(x=> x > 9)

//25. 60